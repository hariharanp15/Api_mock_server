import { AppBar, Button, Container, Toolbar, Typography } from '@mui/material'; import { BrowserRouter, Link, Navigate, Route, Routes, useNavigate } from 'react-router-dom'; import DashboardPage from './pages/DashboardPage'; import ApiListPage from './pages/ApiListPage'; import CreateApiPage from './pages/CreateApiPage'; import EditApiPage from './pages/EditApiPage'; import LoginPage from './pages/LoginPage'; import RegisterPage from './pages/RegisterPage'; import { isAuthenticated, signOut } from './auth';
function Protected({ children }: { children: React.ReactNode }) { return isAuthenticated() ? <>{children}</> : <Navigate to="/login" replace/> }
function Shell(){const navigate=useNavigate();const signedIn=isAuthenticated();const logout=()=>{signOut();navigate('/login')};return <><AppBar position="static"><Toolbar><Typography variant="h6" sx={{flexGrow:1}}>MockAPIs</Typography>{signedIn ? <><Button color="inherit" component={Link} to="/">Dashboard</Button><Button color="inherit" component={Link} to="/apis">API List</Button><Button color="inherit" component={Link} to="/apis/new">Create API</Button><Button color="inherit" onClick={logout}>Sign out</Button></> : <><Button color="inherit" component={Link} to="/login">Sign in</Button><Button color="inherit" component={Link} to="/register">Register</Button></>}</Toolbar></AppBar><Container sx={{py:4}}><Routes><Route path="/" element={<Protected><DashboardPage/></Protected>}/><Route path="/apis" element={<Protected><ApiListPage/></Protected>}/><Route path="/apis/new" element={<Protected><CreateApiPage/></Protected>}/><Route path="/apis/:id/edit" element={<Protected><EditApiPage/></Protected>}/><Route path="/login" element={signedIn?<Navigate to="/" replace/>:<LoginPage/>}/><Route path="/register" element={signedIn?<Navigate to="/" replace/>:<RegisterPage/>}/><Route path="*" element={<Navigate to="/" replace/>}/></Routes></Container></>}; export default function App(){return <BrowserRouter><Shell/></BrowserRouter>}
